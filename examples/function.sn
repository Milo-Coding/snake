reusable_code hello(text name, number times) outputs text {
    number i is times
    loop_while i >? 0 {
        print "hello", name
        i is i - 1
    }
}

reusable_code testFunc() outputs number {
  output 1
}
testFunc()
